import React from "react";
import { AppScreen } from "@/screens/main";
import { Wrapper } from "@/components";

const App: React.FC = () => {
  return (
    <Wrapper>
      <AppScreen />
    </Wrapper>
  );
};

export default App;
