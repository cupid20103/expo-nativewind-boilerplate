import React from "react";
import { NotFoundScreen } from "@/screens/main";
import { Wrapper } from "@/components";

const NotFound: React.FC = () => {
  return (
    <Wrapper>
      <NotFoundScreen />
    </Wrapper>
  );
};

export default NotFound;
